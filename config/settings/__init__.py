"""
Settings package. Choose an environment module explicitly via
DJANGO_SETTINGS_MODULE:

    config.settings.dev    – local development (default for manage.py & tests)
    config.settings.prod   – production (default for wsgi/asgi entrypoints)

This package intentionally exposes no settings itself.
"""
